<odoo>
    <data>
        <record id="view_stock_quant_tree_inventory_editable" model="ir.ui.view">
            <field name="name">stock.quant.inventory.tree.editable.inherit</field>
            <field name="model">stock.quant</field>
            <field name="inherit_id" ref="stock.view_stock_quant_tree_inventory_editable"/>
            <field name="priority">20</field>
            <field name="arch" type="xml">
                <!-- Sobrescribir el campo 'quantity' (si existe) -->
                
                <field name="quantity" position="replace">
                    <field name="quantity" optional="show" decoration-warning="quantity &lt; 0" string="On Hand Quantity"  column_invisible="uid in [9, 7]"/>
                </field>

                <!-- Sobrescribir el campo 'inventory_diff_quantity' (si existe) -->
                <field name="inventory_diff_quantity" position="replace">
                    <field name="inventory_diff_quantity" 
                        string="Difference" 
                        invisible="not inventory_quantity_set" 
                        decoration-muted="inventory_diff_quantity == 0" 
                        decoration-danger="inventory_diff_quantity &lt; 0" 
                        decoration-success="inventory_diff_quantity &gt; 0" 
                        decoration-bf="inventory_diff_quantity != 0"
                        column_invisible="uid in [9, 7]"
                    />
                </field>
            </field>
        </record>
    </data>
</odoo>